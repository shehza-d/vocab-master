export default function WordList() {
  return (
    <div className="word-list">
      <h2>Word List</h2>
      <ul>
        {/* {words.map((word) => (
          <li key={word.id}>{word.arabic}</li>
        ))} */}
      </ul>
    </div>
  );
}
